[phases.setup]
nixPkgs = ["python311Full", "python311Packages.pip"]

[phases.install]
cmds = [
  "pip3 install --upgrade pip",
  "pip3 install -r requirements.txt"
]

[start]
cmd = "sh -c 'python3 main.py & python3 -m uvicorn config_app:app --host 0.0.0.0 --port ${PORT:-8000}'"
